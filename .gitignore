node_modules/
dist/
.shots/
.env
.env.local
.DS_Store
*.log
.vite/
coverage/

# Blender working files. Only exported glTF parts belong in assets/parts/.
*.blend
*.blend1

# fal.ai raw generations, cached so re-running the bake does not re-bill.
# Only the processed PNGs in assets/baked/ are committed.
tools/bake-textures/.cache/
