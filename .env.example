# Needed ONLY by the offline asset bake in tools/. The game itself ships with
# baked assets, makes no network calls, and runs without any of these.

FAL_KEY=
ELEVENLABS_API_KEY=

# Used by tools/bake-catalog to generate item names, descriptions and kitbash
# assemblies. Not required to run or develop the game.
ANTHROPIC_API_KEY=
